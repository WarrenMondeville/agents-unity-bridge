# Ignore everything by default
*

# Except these:
!.gitignore
!README.md
!VALIDATION.md

# Keep test Editor scripts (custom build entry points, etc.)
!Assets/
!Assets/Editor/
!Assets/Editor/*.cs
!Assets/Editor/*.cs.meta

# Keep ProjectSettings directory
!ProjectSettings/
!ProjectSettings/*.txt
!ProjectSettings/*.asset

# But exclude user-specific settings
ProjectSettings/ProjectSettings.asset
ProjectSettings/*.meta

# Keep Packages directory for manifest
!Packages/
!Packages/manifest.json

# Build output artifacts
Builds/
Assets/Scenes/

# .agents-unity-bridge/ is created at runtime by Unity Bridge - always ignore
.agents-unity-bridge/
